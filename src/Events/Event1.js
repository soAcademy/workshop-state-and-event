import React from "react";
import { useLinkClickHandler } from "react-router-dom";
const Event1 =()=>{
  return(
    <button onClick={() => console.log("Hello 555")}>Button 1</button>

  );
};
export default Event1;